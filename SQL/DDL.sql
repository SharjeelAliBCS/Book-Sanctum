create table book
	(isbn			varchar(13), 
	 title			varchar(500),
         description            varchar(5000), 
	 author			varchar(200), 
	 genre                  varchar(200),
	 primary key (isbn)
	);

